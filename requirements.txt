# Minimum versions only — do NOT pin an upper bound so pip installs the
# latest 1.x releases. Tested compatibility target: LangChain 1.x on
# Python 3.10-3.12 (developed against LangChain 1.3.16 / Python 3.12).
streamlit>=1.36
langchain>=1.0.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
python-dotenv>=1.0.1
